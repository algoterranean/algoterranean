
struct LongVector3:
	x as long
	z as long
	y as long
	def constructor(x as long, z as long, y as long):
		self.x = x
		self.z = z
		self.y = y

struct ByteVector3:
	x as byte
	z as byte
	y as byte
	def constructor(x as byte, z as byte, y as byte):
		self.x = x
		self.z = z
		self.y = y
